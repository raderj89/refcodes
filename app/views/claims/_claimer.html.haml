= link_to referral_claims_path(referral), onclick: "var w = window.open(\"#{referral.link}\"); w.focus(); return false;", remote: true, method: :post, class: 'claim-ref' do
  .claim-button
    = image_tag('refcodes.svg')
    .btn-text
      Claim this
