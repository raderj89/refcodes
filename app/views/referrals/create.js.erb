<% if @referral.valid? %>
  $(".js-referrals").prepend("<%= escape_javascript(render(@referral)) %>");
  $(".js-ref-form").html("<%= escape_javascript(render partial: 'form') %>");
<% else %>
  $(".js-ref-form").html("<%= escape_javascript(render 'form') %>");
<% end %>