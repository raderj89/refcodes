<%= form_for @referral, html: { class: 'form-horizontal' } do |f| %>
  <h2>Add Referral</h2>
  <%= render 'shared/error_messages' %>

  <%= f.label :company, "Company" %>
  <%= fields_for :company, @company do |company| %>
      <%= f.text_field :company, class: 'form-control' %>
  <% end %>

  <br>
  
  <%= f.label :details, "Deal details" %> <span class="detail-counter pull-right"></span>
  <%= f.text_area :details, placeholder: "Describe the details of your deal in 140 characters or less", class: 'form-control' %>

  <br>

  <div class="row">
    <div class="col-xs-6">

        <%= f.label :link, "Link to deal" %>
        <%= f.text_field :link, class: 'form-control' %>

    </div>

    <div class="col-xs-6">
      
        <%= f.label :expiration, "Expiration? (Optional)" %>
        <%= f.date_field :expiration, class: 'form-control' %>
    </div>
  </div>

  <br>

  <div class="row">
    <div class="col-xs-6">
        <%= f.label :code, "Referral code (Optional)" %>
        <%= f.text_field :code, class: 'form-control' %>
    </div>

    <div class="col-xs-6">
        <%= f.label :limit, "Limit (Optional)" %>
        <%= f.text_field :limit, class: 'form-control' %>
    </div>
  </div>

  <br>

  <%= f.submit "Submit", class: 'btn btn-lg btn-block btn-primary' %>
<% end %>